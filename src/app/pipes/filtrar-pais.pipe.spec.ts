import { FiltrarPaisPipe } from './filtrar-pais.pipe';

describe('FiltrarPaisPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltrarPaisPipe();
    expect(pipe).toBeTruthy();
  });
});
