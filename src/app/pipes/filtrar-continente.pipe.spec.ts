import { FiltrarContinentePipe } from './filtrar-continente.pipe';

describe('FiltrarContinentePipe', () => {
  it('create an instance', () => {
    const pipe = new FiltrarContinentePipe();
    expect(pipe).toBeTruthy();
  });
});
